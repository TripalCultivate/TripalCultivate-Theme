/**
 * @file
 * Tripal Cultivate Base Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.trpcultivatetheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
