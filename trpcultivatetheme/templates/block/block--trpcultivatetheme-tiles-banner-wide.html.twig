{% extends "block.html.twig" %}
{#
/**
 * @file
 * Tripal Cultivate Theme implementation for a banner wide tile block.
 * Tiie section layout:
 * _______________
 * |      |   |__|
 * |      |___|__|
 * |______|__X___|
 *
 * Available variables:
 * - trpcultivatetheme_tiles_banner_wide: (array) contains raw unformatted values entered into fields.
 *
 * Keys: to access, use . notation ie. trpcultivatetheme_tiles_banner_wide.title for title field value or
 * trpcultivatetheme_tiles_banner_wide.link.url for link url field value.
 *
 * - title: Tile title text.
 * - link: (array) Tile link. This value has link url (url key) and link text value (text key).
 * - coverimg: Tile cover image.
 * - bgcolor: (array) Tile background color. This value has hex value (hex key) and rgb value (rgb key.)
 *
 * @see hook_preprocess_block() implementation in .theme.
 *
 * NOTE: this tile uses bgcolor field to set the tile background colour.
 *
 * Slide layout:
 * <div>
 *   Markup for context menu
 *   Generated by base theme
 * </div>
 *
 * <div>
 *   <a> 
 *     <div> 
 *       Tile Title
 *       <span>Link</span>
 *     </div>
 *   </a> 
 * </div>
 */
#}

{% block content %}
  {# Cover image of the slide #}
  {% set file_url = trpcultivatetheme_tiles_banner_wide.coverimg %}

  <!-- Base theme will append additional markup to this area //-->

  <div class="tripalcultivate-theme-tiles-banner-wide" style="background-image: url('{{ file_url }}'); background-color: {{ trpcultivatetheme_tiles_banner_wide.bgcolor.hex }}">    
    <a href="{{ trpcultivatetheme_tiles_banner_wide.link.url }}" title="{{ trpcultivatetheme_tiles_banner_wide.title}}" target="_blank">
      <div>
        {{ trpcultivatetheme_tiles_banner_wide.title }}
        <span>{{ trpcultivatetheme_tiles_banner_wide.link.text }}</span>  
      </div>
    </a>
  </div>
{% endblock %}